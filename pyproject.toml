[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "rayTracer"
version = "0.0.1"
description = "Ray tracing package for engineering simulations"
readme = "README.md"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["raytracer"]